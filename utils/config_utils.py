"""
Backwards-compatible facade for configuration helpers.

The implementation lives in `config/config_utils.py` so `utils/` stays focused
on small, reusable helpers.
"""

from __future__ import annotations

from config.config_utils import *  # noqa: F403

